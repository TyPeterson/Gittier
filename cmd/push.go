package cmd

// make sure that 'push' command just merges the commit that was made to the main branch

// func Push() error {
// 	// switch to FileTreeBranch, and defer switching back to original branch
// }
